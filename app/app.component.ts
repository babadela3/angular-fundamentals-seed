import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  styleUrls: ['app.component.scss'],
  template: `
    <div class="app">
      <passanger-viewer></passanger-viewer>
    </div>
  `
})
export class AppComponent {
  
}
